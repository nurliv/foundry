# Skill: spec-plan

## Purpose
Support the spec-plan phase for {{project_name}} with traceable graph operations and JSON-first evidence.

## Inputs
- Relevant node IDs (default seed: {{main_spec_id}})
- Current phase objective
- Open risks or constraints
- Spec markdown template: `templates/node-docs/spec.md`

## Outputs
- Updated links/status decisions
- JSON evidence from foundry commands
- Follow-up actions for the next phase (default impact depth: {{default_depth}})
